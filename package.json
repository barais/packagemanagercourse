{
  "name": "versionmanager",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "slidev build",
    "dev": "slidev --open",
    "export": "slidev export",
    "deploygithub": "slidev build --base /packagemanagercourse/ --download && rm -rf docs && mv dist docs && git add docs/** && git commit . -m 'update' && git push"
  },
  "dependencies": {
    "@slidev/cli": "^0.43.15",
    "@slidev/theme-default": "latest",
    "@slidev/theme-seriph": "latest"
  },
  "devDependencies": {
    "playwright-chromium": "^1.39.0"
  }
}
